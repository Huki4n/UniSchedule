<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/UniSchedule;component/Themes/Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Foreground" Value="{StaticResource AppText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CaretBrush" Value="{StaticResource AppText}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid HorizontalAlignment="Stretch">
                            <ScrollViewer x:Name="PART_ContentHost"
                                Margin="12,0,8,0"
                                Padding="0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center"
                                VerticalScrollBarVisibility="Disabled"
                                HorizontalScrollBarVisibility="Disabled"
                                CanContentScroll="False"
                                PanningMode="None"
                                Focusable="False"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CalendarDayButton">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource AppText}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Grid>
                            <Border x:Name="TodayRing" CornerRadius="19" BorderThickness="1.5"
                                    BorderBrush="Transparent" Margin="3"/>
                            <Border x:Name="SelectedBg" CornerRadius="19" Background="Transparent" Margin="4"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SelectedBg" Property="Background" Value="#2A3A55"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="TodayRing" Property="BorderBrush" Value="{StaticResource AppAccent}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectedBg" Property="Background" Value="#2563EB"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="#5B6170"/>
                            </Trigger>
                            <Trigger Property="IsBlackedOut" Value="True">
                                <Setter Property="Opacity" Value="0.35"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CalendarButton">
            <Setter Property="Foreground" Value="{StaticResource AppText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="64"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarButton">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="8,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A3A55"/>
                            </Trigger>
                            <Trigger Property="HasSelectedDays" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2563EB"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CalendarItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarItem">
                        <ControlTemplate.Resources>
                            <DataTemplate x:Key="{x:Static CalendarItem.DayTitleTemplateResourceKey}">
                                <TextBlock Text="{Binding}" FontSize="12" FontWeight="SemiBold"
                                           Foreground="{StaticResource AppMuted}"
                                           HorizontalAlignment="Center" Margin="0,4"/>
                            </DataTemplate>
                        </ControlTemplate.Resources>
                        <Grid x:Name="PART_Root">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="36"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="PART_PreviousButton" Grid.Column="0"
                                        Width="36" Height="36" Cursor="Hand" VerticalAlignment="Center"
                                        Background="Transparent" BorderThickness="0" Padding="0"
                                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                    <Path Data="M 6 0 L 0 6 L 6 12" Width="8" Height="12" Stretch="Fill"
                                          Stroke="{StaticResource AppMuted}"
                                          StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                                <Button x:Name="PART_HeaderButton" Grid.Column="1"
                                        Height="36" Padding="0" VerticalAlignment="Center"
                                        Background="Transparent" BorderThickness="0"
                                        Foreground="{StaticResource AppText}" FontSize="16" FontWeight="SemiBold"
                                        Cursor="Hand" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                                <Button x:Name="PART_NextButton" Grid.Column="2"
                                        Width="36" Height="36" Cursor="Hand" VerticalAlignment="Center"
                                        Background="Transparent" BorderThickness="0" Padding="0"
                                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                    <Path Data="M 0 0 L 6 6 L 0 12" Width="8" Height="12" Stretch="Fill"
                                          Stroke="{StaticResource AppMuted}"
                                          StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                            </Grid>
                            <Grid Grid.Row="1">
                                <Grid x:Name="PART_MonthView" Margin="0,2,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                        <ColumnDefinition Width="38"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="38"/>
                                        <RowDefinition Height="38"/>
                                        <RowDefinition Height="38"/>
                                        <RowDefinition Height="38"/>
                                        <RowDefinition Height="38"/>
                                        <RowDefinition Height="38"/>
                                    </Grid.RowDefinitions>
                                </Grid>
                                <Grid x:Name="PART_YearView" Visibility="Hidden">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="52"/>
                                        <RowDefinition Height="52"/>
                                        <RowDefinition Height="52"/>
                                    </Grid.RowDefinitions>
                                </Grid>
                            </Grid>
                            <Rectangle x:Name="PART_DisabledVisual" Fill="#AA121418" Visibility="Collapsed"
                                       Grid.RowSpan="2" RadiusX="8" RadiusY="8"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Calendar">
            <Setter Property="Background" Value="{StaticResource AppSurface}"/>
            <Setter Property="Foreground" Value="{StaticResource AppText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppBorder}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource {x:Type CalendarDayButton}}"/>
            <Setter Property="CalendarButtonStyle" Value="{StaticResource {x:Type CalendarButton}}"/>
            <Setter Property="CalendarItemStyle" Value="{StaticResource {x:Type CalendarItem}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Calendar">
                        <Border Margin="0,8,8,8" Padding="14,12,14,10"
                                Background="{StaticResource AppSurface}"
                                BorderBrush="{StaticResource AppBorder}"
                                BorderThickness="1" CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <Grid x:Name="PART_Root">
                                <CalendarItem x:Name="PART_CalendarItem"
                                              Style="{TemplateBinding CalendarItemStyle}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{StaticResource AppCard}"/>
            <Setter Property="Foreground" Value="{StaticResource AppText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppBorder}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="SelectedDateFormat" Value="Short"/>
            <Setter Property="CalendarStyle" Value="{StaticResource {x:Type Calendar}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.ColumnSpan="2" Background="{StaticResource AppCard}"
                                    BorderBrush="{StaticResource AppBorder}" BorderThickness="1" CornerRadius="8"/>
                            <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0"
                                               Height="36" Padding="0" Margin="0"
                                               HorizontalAlignment="Stretch"
                                               HorizontalContentAlignment="Left"
                                               VerticalAlignment="Center"
                                               VerticalContentAlignment="Center"
                                               Foreground="{StaticResource AppText}"/>
                            <Button x:Name="PART_Button" Grid.Column="1"
                                    Width="36" Height="36" VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Cursor="Hand" Focusable="False" Padding="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <Path Width="16" Height="16" Stretch="Uniform"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Stroke="{StaticResource AppMuted}" StrokeThickness="1.4"
                                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  Data="M3,5 H13 V13 H3 Z M3,7.2 H13 M5.5,3.2 V5.6 M10.5,3.2 V5.6"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                                   StaysOpen="False"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
</ResourceDictionary>
